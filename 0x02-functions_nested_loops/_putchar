#include <unistd.h>
#include "main.h"
/**
 * _putchar - write the character c to stdout
 * @c : the character to print
 * describtion: using _putcher
 * Return:  1 (Success)
 *          -1 (error)
*/

        int _putch(char c)

        {

return (write(1, &c, 1));
}
