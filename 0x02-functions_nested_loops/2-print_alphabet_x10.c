#include "main.h"

/**
 * main - Entry point
 *
 * Description: .
 *
 * Return: Always 0 (Success)
*/
	void print_alphabet_x10(void);
{
	int line, ch;

	for (line = 0; line <= 'z'; ch++)
	{
		for (ch = 'a'; ch <= 'z'; ch++)

		_putchar(ch);
	_putchar('\n');
	}
}





